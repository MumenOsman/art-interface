This project is going to be an interface for the art decoder project I have done before this one

It will require a server in Go, which makes it possible to use a web interface to input text and display the resulting artwork.